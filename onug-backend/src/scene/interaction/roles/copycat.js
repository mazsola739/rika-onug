exports.copycat = (gameState) => {
  const newGameState = { ...gameState };

  return newGameState;
};

exports.copycat_response = (gameState) => {
  const newGameState = { ...gameState };

  return newGameState;
};
