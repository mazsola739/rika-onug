exports.copycat = (gameState) => {
  const newGameState = { ...gameState };

  return newGameState;
};

exports.copycat_reaction = (gameState) => {
  const newGameState = { ...gameState };

  return newGameState;
};
