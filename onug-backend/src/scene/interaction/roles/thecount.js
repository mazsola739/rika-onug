//TODO doppelganger
exports.thecount = (gameState) => {
  const newGameState = { ...gameState };

  return newGameState;
};

exports.thecount_response = (gameState) => {
  const newGameState = { ...gameState };

  return newGameState;
};
