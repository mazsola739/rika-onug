exports.vampires = (gameState) => {
  const newGameState = { ...gameState };

  return newGameState;
};

exports.vampires_response = (gameState) => {
  const newGameState = { ...gameState };

  return newGameState;
};
