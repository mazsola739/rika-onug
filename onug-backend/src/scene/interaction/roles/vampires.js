exports.vampires = (gameState) => {
  const newGameState = { ...gameState };

  return newGameState;
};

exports.vampires_reaction = (gameState) => {
  const newGameState = { ...gameState };

  return newGameState;
};
