exports.mirrorman = (gameState) => {
  const newGameState = { ...gameState };

  return newGameState;
};

exports.mirrorman_reaction = (gameState) => {
  const newGameState = { ...gameState };

  return newGameState;
};
