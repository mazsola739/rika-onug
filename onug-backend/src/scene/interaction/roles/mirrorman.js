exports.mirrorman = (gameState) => {
  const newGameState = { ...gameState }

  return newGameState
}

exports.mirrorman_response = (gameState) => {
  const newGameState = { ...gameState }

  return newGameState
}
