export const voodoolou = (gameState) => ["voodoolou_kickoff_text"]

//? same as witch
/* if (conditions.hasVoodooLouPlayer(newGameState.players)) {
   const actualSceneRoleTokens = getTokensByOriginalIds(newGameState.players, [70])
    return roles.witch_interaction(newGameState, actualSceneRoleTokens, sceneTitle)
  } */