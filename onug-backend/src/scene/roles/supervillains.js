export const supervillains = (gameState) => ["supervillains_kickoff_text"]

/* if (conditions.hasAnySuperVillainsPlayer) {
    tokens = getTokensByOriginalIds(players, [1])
    return roles.supervillains_interaction(newGameState, tokens, sceneTitle)
  } */
export const supervillain_interaction = (gameState, tokens, title) => {}
export const supervillain_response =  (gameState, token, selected_positions, title) => {}