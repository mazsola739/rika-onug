export const everyonemark = (gameState) => ["everyone_mark_text"]

/* if (conditions.hasMarksPlayer(newGameState.players)) {
   const actualSceneRoleTokens = getAllPlayerTokens(players)
    return roles.everyonemark_interaction(newGameState, actualSceneRoleTokens, sceneTitle)
  } */
export const everyonemark_interaction = (gameState, tokens, title) => {}
