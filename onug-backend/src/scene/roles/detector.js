export const detector = (gameState) => ["detector_kickoff_text"]


//? same as seer
/* if (conditions.hasDetectorPlayer(newGameState.players)) {
   const actualSceneRoleTokens = getTokensByOriginalIds(newGameState.players, [56])
    return roles.seer_interaction(newGameState, actualSceneRoleTokens, sceneTitle)
  } */