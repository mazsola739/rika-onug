{
  "name": "rika-onug",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:frontend": "cd onug-frontend && yarn start",
    "build:frontend": "cd onug-frontend && yarn build",
    "start:backend": "cd onug-backend && yarn start",
    "build:backend": "cd onug-backend && yarn build",
    "install:all": "cd onug-frontend && yarn install && cd ../onug-backend && yarn install",
    "build:all": "yarn build:frontend && yarn build:backend"
  },
  "description": "One Night Ultimate Werewolf",
  "author": "Rika",
  "license": "MIT"
}
